# Model directory

Place the lab-provided ONNX sentiment model here:

```text
model/sentiment.onnx
```

If the model requires tokenizer files, place those files here too, or set `TOKENIZER_NAME` to a Hugging Face tokenizer name/path.
